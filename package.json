{
  "name": "thermite",
  "version": "1.0.0",
  "description": "Disposable one-time use script loading service with memory-only storage and a RESTful API",
  "main": "./dist/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "builderman": "rm -rf ./dist/ ; npx tsc ; node .",
    "build": "npx tsc",
    "start": "node ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.3",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@fastify/cors": "^8.3.0",
    "fastify": "^4.18.0"
  }
}
